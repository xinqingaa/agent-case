# Decomposition Workspaces

每个获准开始的项目在这里拥有一个独立工作区：

```text
workspaces/<project-id>/
├── project.yaml
├── EVIDENCE.md
├── GLOSSARY.md
├── ACCEPTANCE.md
├── detailed/
└── working/
```

`project.yaml` 是范围和权限入口，`EVIDENCE.md` 是证据台账，`detailed/` 是项目事实源，`working/` 只保存临时分析记录。
