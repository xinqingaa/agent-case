# 建设路线图

版本：0.2.0
状态：文档系统重构阶段

## 当前目标

完成仓库级文档、Agent、模板、Schema、脚本和测试体系，并通过不接触真实源码的空项目演练。

## 里程碑

### M0：仓库重构

- [x] 根目录建立 `README.md` 和 `AGENTS.md`。
- [x] 旧拆解与图片进入 `archive/legacy-v0/`。
- [x] 被分析源码由 `code/` 调整为 `sources/`。
- [x] 冻结试点进入 `archive/pilots/`。
- [x] 通用能力从 `decomposition/` 提升到根级目录。

### M1：规范 0.2

- [x] 明确规范、指南、模板和归档的事实源关系。
- [x] 将授权等级升级为等级加能力矩阵。
- [x] 区分静态审阅和运行验证交付状态。
- [ ] 完成人工审阅并冻结 0.2 语义。

### M2：机械验证

- [x] Schema 可以验证 `project.yaml`。
- [x] 校验器可以检查结构、占位符、证据引用和阶段状态。
- [x] 仓库检查可以发现废弃活跃路径和缺失入口。
- [x] 正反 fixtures 覆盖关键失败场景。

### M3：空项目演练

- [x] 初始化虚构项目。
- [ ] 完成 G0 但不读取任何真实源码。
- [ ] 验证未授权运行会被阻断。
- [ ] 验证错误 `[R]`、无来源指标和断裂证据引用会失败。
- [ ] 用户确认文档数量、命名和学习路线。

### M4：单项目试点

只有 M0 至 M3 通过后开始：选择一个低风险项目，先完成 G0/G1，再单独申请运行权限。首个项目通过后修正规范，第二个验证通用性，第三个检查是否过度拟合。

## 暂不实施

- 简洁版自动生成。
- 自研 MCP。
- 多项目并行拆解。
- 自动修改被分析项目。
- 把冻结试点包装成正式样例。
