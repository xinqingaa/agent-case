# 建设路线图

版本：0.3.0
状态：0.3 设计已落地，等待项目授权

## 当前目标

0.3 上游规范正在重构：项目工作区完全平铺，事实底座与学习路线分层，交付按项目目标档案条件式启用。规范冻结前不开始 `imooc-mas`。

## 里程碑

### M0：仓库重构

- [x] 根目录建立 `README.md` 和 `AGENTS.md`。
- [x] 旧拆解与图片进入 `archive/legacy-v0/`。
- [x] 被分析源码由 `code/` 调整为 `sources/`。
- [x] 冻结试点进入 `archive/pilots/`。
- [x] 通用能力从 `decomposition/` 提升到根级目录。

### M1：规范 0.2（历史）

- [x] 明确规范、指南、模板和归档的事实源关系。
- [x] 将授权等级升级为等级加能力矩阵。
- [x] 区分静态审阅和运行验证交付状态。
- [x] 完成人工审阅并冻结 0.2 语义。

### M2：机械验证

- [x] Schema 可以验证 `project.yaml`。
- [x] 校验器可以检查结构、占位符、证据引用和阶段状态。
- [x] 仓库检查可以发现废弃活跃路径和缺失入口。
- [x] 正反 fixtures 覆盖关键失败场景。

### M3：空项目演练（旧结构，历史）

- [x] 初始化虚构项目。
- [x] 完成 G0 但不读取任何真实源码。
- [x] 验证未授权运行会被阻断。能力矩阵全为 false；工具不读取源码。
- [x] 验证错误 `[R]` 和无定义证据引用会失败。无来源指标仍由验收规范人工拦截。
- [x] 用户确认文档数量、命名和学习路线。

### M4：规范 0.3 收尾

- [x] 工作区完全平铺，移除旧目录兼容。
- [x] `learning_profile` 支持按项目选择学习、运行、复刻和二开目标。
- [x] 初始化和校验脚本只处理新平铺结构。
- [x] 空项目验证低目标与高目标的条件式验收。
- [ ] 更新模板验收清单和新项目示例。

### M5：三个项目交付

- `deep-research`：源码学习为主。
- `cloud_agent`：源码学习与运行上手。
- `imooc-mas`：源码学习、运行、最小复刻和二次开发。
- 每完成一个项目，复核规范是否需要调整；三个项目完成后再产出跨项目模式。

## 暂不实施

- 简洁版自动生成。
- 自研 MCP。
- 多项目并行拆解。
- 自动修改被分析项目。
- 把冻结试点包装成正式样例。
