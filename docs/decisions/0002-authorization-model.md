# ADR-0002：授权等级加能力矩阵

状态：接受
日期：2026-08-19

## 背景

单一 L0–L5 等级无法准确表示“可读源码但不可读 Git”“可执行测试但不可联网”“可启动服务但不可读数据库”等现实组合。

## 决定

保留 L0–L5 作为沟通摘要，同时使用布尔能力矩阵作为实际执行边界。缺失或为 `false` 的能力一律禁止。

## 后果

- 授权更精确，可进行机械检查。
- `project.yaml` 更长，但减少了 Agent 自行解释权限的空间。
- 旧项目授权不能直接沿用，必须迁移到新字段。
